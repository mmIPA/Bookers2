<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'shared/userinfoNewbook',locals: { user: @user, book: @book } %>
  　</div>

    <div class="col-md-8 offset-md-1">
      <h1>Books</h1>
        <table class="table">
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
          </tr>
        <% @books.each do |book| %>
          <tr>
            <td><%= image_tag book.user.get_profile_image %></td>
            <td><%= link_to book.title, book_path(book) %></td>
            <td><%= book.body %></td>
          </tr>
        <% end %>
        </table>
    </div>
  </div>
</div>